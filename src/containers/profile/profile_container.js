import React, { Component } from 'react'

export default class Profile extends Component {
  render () {
    var loggedIn = store.getState().user.status === C.SIGNED_IN
    
    return (

    )
  }
}
